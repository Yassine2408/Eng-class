import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Travel English Coaching",
  description: "Practical English for traveling and real-life communication. One-on-one English coaching for adults.",
  keywords: ["English", "Travel", "Learning", "Coaching", "ESL", "Language"],
  authors: [{ name: "Travel English Coaching" }],
  icons: {
    icon: "/logo.svg",
  },
  openGraph: {
    title: "Travel English Coaching",
    description: "Practical English for traveling and real-life communication",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        suppressHydrationWarning
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
